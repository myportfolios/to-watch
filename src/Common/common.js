// import { connect } from "react-redux";

// export const MAKE_API_CALLS = (action) => {
//   return this.props.action(action);
// };

// export default connect(
//   mapStateToProps,
//   { getLatestMovies, getTrendyFilms }
// )(SuggestedMoviesContainer);
